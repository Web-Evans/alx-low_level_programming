#include <stdio.h>

/**
 * main - funtion the prints lower case alphabet in revise.
 * Return: 0 always
 */

int main(void)
{
	char j = 'z';

	for ('j' >= 'a'; j--)
		printf("%c\n", j);
	return (0);
}
